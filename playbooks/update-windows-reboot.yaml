---
- name: Run Windows Update
  hosts: "{{ hosts }}"
  gather_facts: no
  tasks:
    - name: Installer alle tilgængelige opdateringer
      win_updates:
        accept_all_updates: yes
        reboot: yes
        reboot_timeout: 1200
