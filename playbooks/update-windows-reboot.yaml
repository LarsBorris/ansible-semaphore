---
- name: Run Windows Update
  hosts: "{{ hosts }}"
  gather_facts: no
  tasks:
    - name: Installer alle tilgængelige opdateringer
      win_updates:
        category_names:
          - SecurityUpdates
          - CriticalUpdates
        reboot: yes
        reboot_timeout: 3600
