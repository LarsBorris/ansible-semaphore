---
- hosts: "{{ hosts }}"
  become: true
  vars:
    ansible_python_interpreter: /usr/bin/python3
  tasks:
    - name: update apt packages
      apt:
        upgrade: yes
        update_cache: yes
        cache_valid_time: 3600